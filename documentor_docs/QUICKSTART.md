Based on the provided code snippets, there is **insufficient information** to generate a complete setup guide. The snippets do not contain explicit installation commands, package manager details, or environment variable configurations.

Below is the `QUICKSTART.md` strictly containing only the verified details extracted from the context.

---

# Quickstart Guide

## Project Information

Based on the repository configuration, this project uses **Next.js**.

### Known Dependencies
* `next` (Next.js framework)

---

## Model Configurations

The repository defines the following video generation model configurations:

* **PixVerse 6**: `pixverse-6` (`pixverse/v6/text-to-video`)
* **Kling 2.6**: `kling-2.6` (`kling-video/v2.6/pro/text-to-video`)
* **DoP**: `dop` (`higgsfield-ai/dop/lite`)
* **LTX 2.5 Pro**: `ltx-2.5-pro` (`lightricks/ltx-2.5/text-to-video/pro`)

---

> **Note:** Detailed setup steps (e.g., `npm install` or `yarn install`), environment variables (`.env`), and start commands are not specified in the provided context snippets.